// The below is the Pseudo-Code for what I want to do. We will see if I am actually able to accomplish any of it

User loads page, fires the following, non-user-initiated: 

	function(fadeIn)_donCorleoneHdr
	function(soundIn)_godfatherTheme
	
		if user_clicks "partII",
			function(partII_header).slideLeft
		else slideLeft_after_10secs

	newsletter_Sign_up
		if user_submits,
			add to email_list;
		else if invalid_email,
			text."You must enter a valid email address"
		else doNothing;